// IMPORT PACKAGE REFERENCES
import React from 'react';

// IMPORT PROJECT REFERENCES
import {DirectDriver} from "../DirectDriver/DirectDriver";

// COMPONENT

export const DirectDriverPage = () => (
    <div className="p-3">
        <DirectDriver />
    </div>
);